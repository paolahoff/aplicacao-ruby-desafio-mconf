<!DOCTYPE html>

<html lang="pt-BR">

<head>
  <title> Login </title>
  <style>
    body{
      background: linear-gradient(-105deg, #FF9E00, #9D4EDD, #5A189A);
      background-size: 100% 100%;
      height: 100vh;
      display: flex;
      align-items: center;
      flex-direction: column;
      justify-content: space-between;
      padding: 0;
      margin: 0;
    }
    .forgotPwdBox{
      background-color: white;
      width: 300px;
      padding: 40px;
      border-radius: 50px;
      box-shadow: 20px 30px 40px;
    }

    .forgotPwdBox h1{
      color: #240046;
      text-align: center;
      margin: 10px 0 30px;
      font-family: Arial Black, sans-serif;
    }
    .forgotPwdBox .field input{
      width: 100%;
      padding: 10px;
      font-size: 16px;
      color: gray;
      letter-spacing: 2px;
      border: none;
      border-bottom: 1px solid #240046;
      margin-bottom: 20px;
    }
    
    .forgotPwdBox .field label{
      color: #240046;
      font-family: Arial, sans-serif;
      font-size: 14px;
    }

    .forgotPwdBox .actions input{
      width: 100%;
      height: 40px;
      margin-bottom: 20px;
      border: none;
      outline: none;
      color: white;
      background: linear-gradient(-105deg, #FF9E00, #9D4EDD, #5A189A);
      padding: 10px 20px;
      font-size: 14px;
      cursor: pointer;
      border-radius: 10px;
    }

    .forgotPwdBox a{
      text-decoration: none;
      align-items: right;
      cursor: pointer;
    }

  </style>
</header>

<body>
  <div> </div>
  <div class="forgotPwdBox">
    <h1>Forgot your password?</h1>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>

      <div class="actions">
        <%= f.submit "Send me reset password instructions" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
  <div></div>
</body>
</html>
